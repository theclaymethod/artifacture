import { DiagramWalkthrough, ExplainerShell, Section } from '../../visual-explainer-mdx/components';

export default function RequestWalkthrough() {
  return (
    <ExplainerShell title="A cache miss, step by step" summary="On a cache miss, the database supplies the record returned to the caller." reviewTools={false}>
      <DiagramWalkthrough title="Request handling on a cache miss" layout="flow" direction="horizontal"
          nodes={[
            { id: 'request', label: 'Request', detail: 'GET /items/:id' },
            { id: 'cache', label: 'Cache', detail: 'No current entry' },
            { id: 'database', label: 'Database', detail: 'Read the record' },
            { id: 'response', label: 'Response', detail: 'Return the record' },
          ]}
          edges={[
            { id: 'check-cache', from: 'request', to: 'cache' },
            { id: 'read-record', from: 'cache', to: 'database', label: 'miss' },
            { id: 'return-record', from: 'database', to: 'response' },
          ]}
          steps={[
            { edgeId: 'check-cache', caption: 'Look up the requested record in the cache.' },
            { edgeId: 'read-record', caption: 'No cached entry exists. Read the record from the database.' },
            { edgeId: 'return-record', caption: 'Return the record to the caller.' },
          ]}
      />
      <Section title="Sequence and timing">
        <p>The point follows the cache-miss path in order. Animation timing does not represent network latency. A cache hit would follow a different path.</p>
      </Section>
    </ExplainerShell>
  );
}
