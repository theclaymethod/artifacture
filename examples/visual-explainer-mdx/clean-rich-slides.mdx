import { CodeBlock, MermaidBlock, Pipeline, Slide, SlideDeck } from '../../visual-explainer-mdx/components';

<SlideDeck title="A cache hit is a branch" reviewTools={false}>
  <Slide title="Repeated reads need not repeat the work">
    <p>This example caches immutable catalog versions. A version in the key keeps one published catalog distinct from the next.</p>
  </Slide>
  <Slide title="Check, load, retain">
    <Pipeline steps={[
      {title:'Check',body:'Look up the versioned catalog key.'},
      {title:'Load',body:'Read the origin only when absent.'},
      {title:'Retain',body:'Store the result for the next request.'}
    ]} />
  </Slide>
  <Slide title="Presence is not truthiness">
    <CodeBlock language="ts" code={`const cached = cache.get(key);
if (cached !== undefined) return cached;
const catalog = await readOrigin(key);
cache.set(key, catalog);
return catalog;`} highlightLines={[2]} />
  </Slide>
  <Slide title="A miss has more work to do">
    <MermaidBlock chart={`flowchart TD
      Request --> Lookup
      Lookup -->|hit| Return
      Lookup -->|miss| Origin
      Origin --> Store
      Store --> Return`} />
  </Slide>
  <Slide title="Decide what this example leaves open">
    <p>Concurrent misses can still issue duplicate reads. Memory also needs a bound. Coalescing requests and evicting old versions are separate decisions from the basic hit/miss branch.</p>
  </Slide>
</SlideDeck>
