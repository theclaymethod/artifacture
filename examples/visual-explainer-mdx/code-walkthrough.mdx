import { CodeBlock, ExplainerShell, Section } from '../../visual-explainer-mdx/components';

<ExplainerShell title="Count a cache hit without changing its value" summary="A tiny lookup function shows why presence and truthiness are different questions." preset="algebrica" reviewTools={false}>
  <Section title="Keep the branch explicit">
    <CodeBlock language="ts" filename="Illustrative cache.ts" highlightLines={[3,4]} annotations={[
      {line:3,note:'Map.get returns undefined for a missing key in this example.'},
      {line:4,note:'A stored zero still counts as a hit.'}
    ]} code={`const cache = new Map<string, number>();
export function read(key: string) {
  const value = cache.get(key);
  const found = value !== undefined;
  return { found, value };
}`} />
    <p>The return value keeps presence separate from data. A caller can display zero without issuing another read. Because this map accepts numbers only, undefined remains an unambiguous missing-value sentinel.</p>
  </Section>
</ExplainerShell>
