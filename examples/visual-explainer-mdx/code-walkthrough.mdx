import { CodeBlock, ExplainerShell, Section } from '../../visual-explainer-mdx/components';

<ExplainerShell title="Recognize a cached zero" summary="For a map of numbers, undefined means missing; zero is a valid result." preset="algebrica" reviewTools={false}>
  <Section title="Test the missing-value sentinel">
    <CodeBlock language="ts" filename="Illustrative cache.ts" highlightLines={[3,4]} annotations={[
      {line:3,note:'Map.get returns undefined for a missing key in this example.'},
      {line:4,note:'A stored zero still counts as a hit.'}
    ]} code={`const cache = new Map<string, number>();
export function read(key: string) {
  const value = cache.get(key);
  const found = value !== undefined;
  return { found, value };
}`} />
    <p>The caller receives both found and value, so a cached zero needs no further read. Because the map accepts only numbers, undefined unambiguously means the key is missing.</p>
  </Section>
</ExplainerShell>
