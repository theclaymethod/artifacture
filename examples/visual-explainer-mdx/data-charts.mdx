import { LieflatChart } from '../../visual-explainer-mdx/components';
import { repairOutcomes, repairDevices, repairTimeline, repairFaults, repairPaths } from './repair-story-data';
import './repair-story.css';

export default function RepairStory() {
  return (
    <main className="repair-story" data-ve-preset="lieflat">
      <header className="repair-story-header">
        <h1>June at the repair bench</h1>
        <p>A neighborhood repair bench received 72 devices in June. Fifty-four were repaired, ten awaited parts, and eight were beyond repair.</p>
        <p className="repair-story-source">Illustrative workshop records.</p>
      </header>
      <section className="repair-story-opening">
        <LieflatChart title="Three quarters repaired" spec={repairOutcomes} />
        <LieflatChart title="Lamps were most common" spec={repairDevices} />
      </section>
      <section className="repair-story-section">
        <div className="repair-story-aside">
          <h2>The busiest day</h2>
          <p>June 20 had seven arrivals, the month’s peak. Most days brought one or two.</p>
        </div>
        <LieflatChart title="Daily arrivals in June" spec={repairTimeline} />
      </section>
      <section className="repair-story-section">
        <div className="repair-story-aside">
          <h2>Which faults were repairable?</h2>
          <p>All 43 devices with cable or switch faults were repaired. Circuit faults accounted for the ten awaiting parts and the eight beyond repair.</p>
        </div>
        <LieflatChart title="Faults by device" spec={repairFaults} />
      </section>
      <section className="repair-story-paths">
        <LieflatChart title="Device, fault, and outcome" description="Each thread traces one of the 72 devices." spec={repairPaths} />
      </section>
      <footer className="repair-story-footer">
        <p>Edit the records to update all five charts. Render them with <code>LieflatChart</code> or the JSON chart command.</p>
        <a href="https://github.com/larashero3-dotcom/lieflat-charts">Visual direction: Lieflat Charts</a>
      </footer>
    </main>
  );
}
