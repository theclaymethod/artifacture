import { DiagramCanvas, ExplainerShell, Section } from '../../visual-explainer-mdx/components';

export default function RequestFlow() {
  return (
    <ExplainerShell title="Cache hits and misses" summary="The API checks the cache before reading the database. On a miss, it stores the result so a later request can reuse it." reviewTools={false}>
      <Section title="Request flow">
        <DiagramCanvas title="Request handling with a cache" layout="flow"
          nodes={[
            { id: 'request', label: 'Request', detail: 'GET /items/:id' },
            { id: 'cache', label: 'Cached result?', shape: 'diamond' },
            { id: 'database', label: 'Read database', detail: 'Fetch the current record' },
            { id: 'store', label: 'Store result', detail: 'Set a bounded cache lifetime' },
            { id: 'response', label: 'Send response', detail: 'Return the record to the caller' },
          ]}
          edges={[
            { from: 'request', to: 'cache' },
            { from: 'cache', to: 'response', label: 'hit' },
            { from: 'cache', to: 'database', label: 'miss' },
            { from: 'database', to: 'store' },
            { from: 'store', to: 'response' },
          ]}
        />
      </Section>
      <Section title="A miss reads and stores the record">
        <p>A miss adds a database read and a cache write. Later requests can reuse the entry until it expires or is invalidated. The diagram shows the sequence, without a measured speedup.</p>
      </Section>
    </ExplainerShell>
  );
}
