import { CodeBlock, DiagramCanvas, DiffBlock, ExplainerShell, Quiz, Section } from '../../visual-explainer-mdx/components';

<ExplainerShell title="A cache adds an early return" summary="An example change to an immutable catalog reader: repeat requests reuse the same version instead of loading it again." reviewTools={false}>
  <Section title="The old path always reads the origin">
    <DiagramCanvas nodes={[{id:'request',label:'Request',shape:'oval'},{id:'read',label:'Read origin'},{id:'return',label:'Return catalog'}]} edges={[{from:'request',to:'read'},{from:'read',to:'return'}]} />
  </Section>
  <Section title="The new path distinguishes a hit from a miss">
    <DiagramCanvas layout="flow" nodes={[{id:'request',label:'Request',shape:'oval'},{id:'lookup',label:'Version cached?',shape:'diamond'},{id:'hit',label:'Return cached catalog'},{id:'miss',label:'Read and cache catalog'}]} edges={[{from:'request',to:'lookup'},{from:'lookup',to:'hit',label:'hit'},{from:'lookup',to:'miss',label:'miss'}]} />
  </Section>
  <Section title="The early return is the behavior change">
    <DiffBlock language="ts" filename="Illustrative catalog.ts" mode="split" before={`export async function loadCatalog(version: string) {
  return readOrigin(version);
}`} after={`export async function loadCatalog(version: string) {
  const cached = cache.get(version);
  if (cached !== undefined) return cached;
  const catalog = await readOrigin(version);
  cache.set(version, catalog);
  return catalog;
}`} />
    <CodeBlock language="ts" code={`if (cached !== undefined) return cached;`} annotations={[{line:1,note:'A hit leaves before the origin read.'}]} />
    <p>The key contains the immutable catalog version. This avoids mixing published versions, but it does not bound memory or coalesce simultaneous misses. Those policies need separate decisions.</p>
  </Section>
  <Section title="Check the read path">
    <Quiz questions={[{q:'What does a cache hit skip?',options:[{text:'The origin read.',correct:true,why:'The early return occurs before readOrigin.'},{text:'The cache lookup.',why:'Every request must look up its version before it can be classified as a hit.'}]},{q:'Does this patch prevent simultaneous misses from loading twice?',options:[{text:'No; both may reach readOrigin.',correct:true,why:'No in-flight request is stored or shared.'},{text:'Yes; cache.set runs eventually.',why:'An eventual value does not coordinate two earlier reads.'}]}]} />
  </Section>
</ExplainerShell>
