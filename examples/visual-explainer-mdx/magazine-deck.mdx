import { DecisionMatrix, Slide, SlideDeck } from '../../visual-explainer-mdx/components';

<SlideDeck orientation="horizontal" title="What a cache remembers" preset="algebrica" reviewTools={false}>
  <Slide title="A fast answer can be an old answer">
    <p>A cache stores a previous result so a later request can avoid repeating work. The useful question is how long that result remains valid.</p>
  </Slide>
  <Slide title="Three ways to define freshness">
    <DecisionMatrix rows={[
      {Policy:'Time to live',Rule:'Expire after a duration',Tradeoff:'Changes may remain invisible until expiry.'},
      {Policy:'Invalidation',Rule:'Remove entries after writes',Tradeoff:'Every relevant write must send the signal.'},
      {Policy:'Versioned keys',Rule:'Use a new key for a new version',Tradeoff:'Old entries still need eventual cleanup.'}
    ]} />
  </Slide>
  <Slide title="Choose from the cost of staleness">
    <p>A generated image can tolerate a different freshness policy from an account balance. Start with what the reader or caller needs to be true, then decide what the cache is allowed to remember.</p>
  </Slide>
</SlideDeck>
