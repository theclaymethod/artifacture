import { DecisionMatrix, Slide, SlideDeck } from '../../visual-explainer-mdx/components';

<SlideDeck orientation="horizontal" title="Cache freshness policies" preset="algebrica" reviewTools={false}>
  <Slide title="How long is a cached result valid?">
    <p>A cache reuses an earlier result. Its freshness policy determines when a request must obtain a new one.</p>
  </Slide>
  <Slide title="Three ways to define freshness">
    <DecisionMatrix rows={[
      {Policy:'Time to live',Rule:'Expire after a duration',Tradeoff:'Changes may remain invisible until expiry.'},
      {Policy:'Invalidation',Rule:'Remove entries after writes',Tradeoff:'Every relevant write must send the signal.'},
      {Policy:'Versioned keys',Rule:'Use a new key for a new version',Tradeoff:'Old entries still need eventual cleanup.'}
    ]} />
  </Slide>
  <Slide title="Choose the acceptable staleness">
    <p>A generated image and an account balance may tolerate different periods of staleness. Set that requirement before choosing expiry, invalidation, or versioned keys.</p>
  </Slide>
</SlideDeck>
