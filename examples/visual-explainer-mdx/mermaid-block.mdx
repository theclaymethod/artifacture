import { ExplainerShell, MermaidBlock, Section } from '../../visual-explainer-mdx/components';

<ExplainerShell title="Job states and retry limits" summary="Temporary failures wait for a scheduled retry. Invalid input and exhausted retry budgets go to quarantine." reviewTools={false}>
  <Section title="From queue to completion or quarantine">
    <MermaidBlock chart={`stateDiagram-v2
  [*] --> Queued
  Queued --> Running: claim
  Running --> Complete: success
  Running --> Waiting: temporary failure
  Waiting --> Queued: delay elapsed
  Running --> Quarantined: invalid input or exhausted budget
  Quarantined --> Queued: repair and replay
  Complete --> [*]`} />
    <p>Waiting leaves the job incomplete. Quarantine retains its identity and failure context. After repair, manual replay uses the normal queue, claiming rules, and idempotency checks.</p>
  </Section>
</ExplainerShell>
