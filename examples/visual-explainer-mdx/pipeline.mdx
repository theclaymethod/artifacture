import { Callout, DecisionMatrix, ExplainerShell, Pipeline, RiskLedger, Section } from '../../visual-explainer-mdx/components';

export default function PipelineExplainer() {
  return (
    <ExplainerShell
      eyebrow="MDX canonical source"
      title="Visual Explainer Pipeline"
      summary="A component-authored explainer can be reviewed interactively, revised from annotations, and exported as generated standalone HTML."
    >
      <Section kicker="flow" title="Authoring Loop">
        <Pipeline
          steps={[
            { title: 'Prompt', body: 'The coding agent receives an explainer request and chooses MDX by default.' },
            { title: 'MDX source', body: 'Content uses reusable React components instead of raw HTML templates.' },
            { title: 'Preview', body: 'The page can be loaded in an Agent Pages-style React shell for annotation.' },
            { title: 'Export', body: 'The source is bundled into one generated HTML artifact.' },
          ]}
        />
      </Section>

      <Section kicker="decision" title="Format Boundary">
        <DecisionMatrix
          rows={[
            { format: 'MDX', use: 'Structured explainers', proof: 'This file exports to HTML' },
            { format: 'TSX', use: 'Custom stateful interactions', proof: 'interactive.tsx sample exports to HTML' },
            { format: 'HTML', use: 'Final portable artifact only', proof: 'Generated by the exporter' },
          ]}
        />
      </Section>

      <Section kicker="gate" title="Completion Risks">
        <RiskLedger
          risks={[
            {
              risk: 'Raw HTML relapse',
              signal: 'Agents edit final HTML directly',
              mitigation: 'Make source MDX/TSX the editable artifact',
              level: 'high',
            },
            {
              risk: 'Weak proof',
              signal: 'Exporter works only for one happy path',
              mitigation: 'Verify MDX and React-heavy samples separately',
              level: 'medium',
            },
            {
              risk: 'Video drift',
              signal: 'Hyperframes path remains HTML-template-only',
              mitigation: 'Add video parity as a later verified slice',
              level: 'medium',
            },
          ]}
        />
      </Section>

      <Section kicker="contract" title="Generated Artifact">
        <Callout>
          The exported HTML is a build artifact. The durable authoring source is this MDX file.
        </Callout>
      </Section>
    </ExplainerShell>
  );
}
