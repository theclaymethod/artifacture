import { DiagramCanvas, ExplainerShell, Section } from '../../visual-explainer-mdx/components';

export default function VisualLanguages() {
  const themes = [
    { id: 'lieflat', title: 'Lieflat', text: 'Countable units, dated observations, and individual paths on gray paper, drawn in charcoal.' },
    { id: 'algebrica', title: 'Algebrica', text: 'Serif type on warm stone for definitions, proofs, and longer explanations.' },
    { id: 'mono-color', title: 'Mono Color', text: 'Cobalt on neutral paper, with a second ink for emphasis.' },
  ];
  return (
    <ExplainerShell title="Compare the three themes" summary="The same source-to-HTML diagram in Lieflat, Algebrica, and Mono Color." reviewTools={false}>
      {themes.map((theme) => (
        <div key={theme.id} data-ve-preset={theme.id} style={{ background: 'var(--ve-bg)', color: 'var(--ve-text)', padding: 'clamp(20px, 4vw, 48px)' }}>
          <Section title={theme.title}>
            <p style={{ marginBottom: 28, maxWidth: '65ch', fontSize: 'var(--ve-shell-summary-size)', lineHeight: 1.6 }}>{theme.text}</p>
            <DiagramCanvas title="Source to HTML" nodes={[{ id: 'source', label: 'Source', detail: 'Editable content' }, { id: 'render', label: 'Render', detail: 'Apply the theme' }, { id: 'read', label: 'Read', detail: 'Inspect the result in a browser' }]} edges={[{ from: 'source', to: 'render' }, { from: 'render', to: 'read' }]} />
          </Section>
        </div>
      ))}
    </ExplainerShell>
  );
}
