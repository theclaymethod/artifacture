import { Pipeline, Slide, SlideDeck } from '../../visual-explainer-mdx/components';

<SlideDeck title="Bound retries and preserve failed jobs" reviewTools={false}>
  <Slide title="Stop retrying invalid work">
    <p>Retries can recover from a temporary outage. An unlimited retry loop can keep invalid jobs running indefinitely and obscure the need for repair.</p>
  </Slide>
  <Slide title="Classify, delay, or quarantine">
    <Pipeline steps={[
      {title:'Classify',body:'Is the failure temporary or permanent?'},
      {title:'Delay',body:'Retry temporary failures within a fixed budget.'},
      {title:'Retain',body:'Keep exhausted or invalid jobs for inspection.'}
    ]} />
  </Slide>
  <Slide title="Preserve identity through replay">
    <p>A timeout can hide a successful side effect. Reconcile the outcome before replay, using the original idempotency key for the same business operation.</p>
  </Slide>
</SlideDeck>
