import { Pipeline, Slide, SlideDeck } from '../../visual-explainer-mdx/components';

<SlideDeck title="Give retries a stopping point" reviewTools={false}>
  <Slide title="Repeated failure is still failure">
    <p>Retrying can recover from a temporary outage. Without a limit, the same mechanism can keep invalid work alive indefinitely and hide the jobs that need repair.</p>
  </Slide>
  <Slide title="Make the next step explicit">
    <Pipeline steps={[
      {title:'Classify',body:'Is the failure temporary or permanent?'},
      {title:'Delay',body:'Retry temporary failures within a fixed budget.'},
      {title:'Retain',body:'Keep exhausted or invalid jobs for inspection.'}
    ]} />
  </Slide>
  <Slide title="Preserve identity through replay">
    <p>A timeout can hide a successful side effect. Keep the original idempotency key and reconcile the outcome before repeating the action. A new attempt should not create a new business operation.</p>
  </Slide>
</SlideDeck>
