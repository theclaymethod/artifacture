import { DiagramCanvas, ExplainerShell, Section } from '../../visual-explainer-mdx/components';

<ExplainerShell title="A cache changes the read path" summary="A hit returns a stored value. A miss reads the origin and stores the result for the next request." reviewTools={false}>
  <Section title="Two possible outcomes">
    <DiagramCanvas layout="flow"
          direction="vertical" description="Requests check the cache. Hits return immediately; misses read the origin."
      nodes={[
        {id:'request',label:'Request',shape:'oval'},
        {id:'lookup',label:'Value cached?',shape:'diamond'},
        {id:'hit',label:'Return cached value'},
        {id:'origin',label:'Read origin'},
        {id:'store',label:'Cache and return'}
      ]}
      edges={[
        {from:'request',to:'lookup'},
        {from:'lookup',to:'hit',label:'hit'},
        {from:'lookup',to:'origin',label:'miss'},
        {from:'origin',to:'store'}
      ]} />
  </Section>
  <Section title="Freshness is a separate decision">
    <p>This example describes cache-aside reads. It does not determine when a stored value becomes stale. A real cache needs an expiry policy, invalidation after writes, or a key that changes with the source version.</p>
  </Section>
</ExplainerShell>
